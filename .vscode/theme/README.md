# Porch Logic Repo Theme

This theme lives inside the repo so it stays versioned with the project.

Install (VS Code):
1. Open the Command Palette.
2. Run "Developer: Install Extension from Location...".
3. Select this folder: `.vscode/theme`.
4. Reload VS Code when prompted.

When this theme changes, reinstall it the same way so VS Code picks up updates.
